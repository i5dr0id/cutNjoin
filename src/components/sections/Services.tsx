import { Section } from "@/components/primitives";
import { sections } from "@/lib/site";

export function Services() {
  return <Section id={sections.services} eyebrow="What we do" heading="Our Services" />;
}
